worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;
    
    sendfile        on;
    keepalive_timeout  65;

    server {
        listen 80;

        # Serve frontend from /
        location / {
            proxy_pass https://ory-kratos-frontend.onrender.com/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Serve backend API from /api/
        location /api/ {
            proxy_pass https://ory-kratos-render.onrender.com/;
            
            # 🔴 FIX: Allow insecure SSL for debugging
            proxy_ssl_verify off;    
            
            # 🔴 FIX: Enable SNI (Server Name Indication)
            proxy_ssl_server_name on; 
            
            # 🔴 FIX: Allow only modern TLS protocols
            proxy_ssl_protocols TLSv1.2 TLSv1.3; 

            # 🔴 FIX: Use strong SSL ciphers
            proxy_ssl_ciphers HIGH:!aNULL:!MD5; 

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}
